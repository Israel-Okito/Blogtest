// import { client } from "../../sanity/lib/client";



// export const getPageMetadataById = async (slug) => {
//     // Utilisez le client Sanity (ou votre source de données) pour récupérer les métadonnées
//     const metadata = await client.fetch(`
//     *[type == "post" && slug.current == "${slug}"][0] {
//         title,
//         excerpt,
//         _id
//       } 
//       `, {slug });
  
//     return metadata;
//   };

